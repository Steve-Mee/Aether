import { WebsitePagesView } from '@/features/website';

/** CMS entry for site pages — same data/hooks as `/website/pages`. */
export default function PagesCms() {
  return <WebsitePagesView variant="cms" />;
}
